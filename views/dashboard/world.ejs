<%- include('../partials/header') %>

<script>
  // Evitar que la página se cargue desde cache
  window.onpageshow = function(event) {
    if (event.persisted) {
      window.location.reload();
    }
  };
</script>

<%- include('../partials/navbar') %>

<main class="container-fluid py-4">
  <div class="d-flex justify-content-between align-items-center mb-4 px-3">
    <div>
      <h1 class="fw-bold text-primary">🌍 Global Activity Overview</h1>
      <p class="text-muted">Visualize user locations and activity worldwide</p>
    </div>
    <button id="themeToggle" class="btn btn-outline-secondary">🌙 Toggle Theme</button>
  </div>

  <div class="grid-stack px-3">
    <!-- Aquí se insertarán globo 3D, mapa con burbujas, mapa de calor -->
  </div>
</main>

<%- include('../partials/footer') %>
